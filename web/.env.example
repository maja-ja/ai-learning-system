# Vite 前端（web/）— 會員與計費
# 複製為 .env.local 後填入真值；勿 commit .env.local

# Clerk（僅 publishable，不可放 secret）
VITE_CLERK_PUBLISHABLE_KEY=pk_test_xxx

# Next 計費站（kadusella）本機預設 :3000
VITE_BILLING_BASE_URL=http://localhost:3000

# FastAPI；與後端同源時可省略，由 Vite proxy 或相對路徑處理
# VITE_API_BASE_URL=http://127.0.0.1:8000
